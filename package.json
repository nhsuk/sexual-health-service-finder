{
  "name": "sexual-health-service-finder",
  "version": "0.11.0",
  "description": "Helping to connect people to sexual health services.",
  "main": "app.js",
  "scripts": {
    "brunch-build": "yarn brunch build",
    "ci-hook": "yarn lint && yarn coverage-generate && yarn coverage-check",
    "coverage-check": "istanbul check-coverage --config .istanbul.yml",
    "coverage-check-unit": "yarn coverage-generate-unit && istanbul check-coverage --config .istanbul.unit.yml",
    "coverage-generate": "yarn istanbul -- cover _mocha -- --exit --recursive",
    "coverage-generate-unit": "yarn istanbul cover -- _mocha -- --exit --recursive test/unit",
    "coverage-generate-full": "yarn istanbul -- cover --include-all-sources _mocha -- --exit --recursive",
    "coverage-generate-no-metrics": "yarn istanbul -- cover _mocha -- --exit \"./test/**/!(metrics).js\"",
    "coverage-upload-coveralls": "cat ./coverage/lcov.info | coveralls",
    "git-hook": "yarn lint && yarn coverage-generate-unit && yarn coverage-check-unit",
    "header-build": "node headerItems.js",
    "istanbul": "NODE_ENV=test istanbul",
    "lint": "yarn lint-backend && yarn lint-frontend",
    "lint-backend": "eslint --ext .js,.json .",
    "lint-frontend": "eslint --no-ignore --no-eslintrc ./app/public/js",
    "lint-watch": "esw --watch .",
    "postrewrite": "yarn git-hook",
    "precommit": "yarn git-hook",
    "prepush": "yarn git-hook",
    "start-watch": "yarn header-build && yarn brunch watch & nodemon app.js | ./node_modules/bunyan/bin/bunyan",
    "test": "yarn header-build && NODE_ENV=test mocha --exit --recursive test",
    "test-ci": "yarn ci-hook && yarn coverage-upload-coveralls",
    "test-unit": "yarn header-build && NODE_ENV=test mocha --recursive test/unit",
    "test-unit-watch": "yarn header-build && NODE_ENV=test mocha --recursive test/unit --reporter min --watch",
    "test-integration-watch": "yarn header-build && NODE_ENV=test mocha --recursive test/integration --reporter min --watch",
    "test-watch": "yarn test -- --reporter min --watch"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nhsuk/sexual-health-service-finder.git"
  },
  "keywords": [
    "sexual",
    "health",
    "services",
    "testing",
    "online",
    "NHS",
    "chlamydia"
  ],
  "author": "Mark Harrop",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/nhsuk/sexual-health-service-finder/issues"
  },
  "homepage": "https://github.com/nhsuk/sexual-health-service-finder#readme",
  "dependencies": {
    "after-brunch": "^0.0.5",
    "body-parser": "^1.18.2",
    "brunch": "2.10.12",
    "compression": "^1.7.1",
    "cookie-parser": "^1.4.3",
    "elasticsearch": "^14.2.0",
    "express": "^4.16.2",
    "express-prom-bundle": "^3.3.0",
    "helmet": "^3.11.0",
    "moment": "^2.22.0",
    "nhsuk-bunyan-logger": "^1.7.0",
    "node-sass": "4.9.0",
    "nunjucks": "3.1.2",
    "postcodesio-client": "^0.3.3",
    "querystring": "^0.2.0",
    "request": "2.85.0",
    "rewire": "^4.0.0",
    "sass-brunch": "2.10.4",
    "sinon": "^5.0.0",
    "uglify-es": "^3.1.8",
    "verror": "^1.10.0"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "chai-http": "^4.0.0",
    "cheerio": "^1.0.0-rc.2",
    "coveralls": "^3.0.0",
    "destroy": "^1.0.4",
    "eslint": "^4.6.1",
    "eslint-config-airbnb-base": "^13.0.0",
    "eslint-config-nhsuk": "^0.15.0",
    "eslint-plugin-import": "^2.7.0",
    "eslint-plugin-json": "^1.2.0",
    "eslint-plugin-mocha": "^5.0.0",
    "eslint-watch": "^3.1.2",
    "husky": "^0.14.2",
    "istanbul": "^1.1.0-alpha.1",
    "mocha": "^5.0.0",
    "nock": "^9.0.14",
    "nodemon": "^1.12.0",
    "proxyquire": "^2.0.0"
  }
}
